{
  "name": "backend",
  "version": "1.0.0",
  "description": "REST API backend for pet dashboard",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "build": "echo 'No build step required for JavaScript backend'",
    "lint": "echo 'No linter configured yet'",
    "typecheck": "echo 'No typecheck configured yet'",
    "test": "DOTENV_CONFIG_PATH=.env.test vitest run",
    "test:watch": "DOTENV_CONFIG_PATH=.env.test vitest"
  },
  "keywords": [
    "express",
    "postgres",
    "rest-api"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "pg": "^8.20.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^7.2.2",
    "vitest": "^4.1.6"
  }
}
