{
	email {$ACME_EMAIL}
}

{$SITE_DOMAIN} {
	encode gzip

	handle /api/* {
		reverse_proxy backend:4000
	}

	handle /health {
		reverse_proxy backend:4000
	}

	handle {
		root * /srv
		file_server
		try_files {path} /index.html
	}
}
